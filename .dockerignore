# Build artifacts
bin/
data/
*.db
*.db-wal
*.db-shm

# Make
Makefile

# IDE
.idea/
.vscode/
.claude/
*.swp
*.swo

# Git
.git/
.gitignore

# Docs (not needed in image)
docs/
*.md
!README.md

# Test files (allow test/e2e/ for E2E tests, exclude other test directories)
test/integration/
# Exclude test results but keep the directory structure
test/e2e/results/
# Keep general test exclusion for package-level tests
pkg/**/*_test.go

# Local config
*.yaml
*.yml
!geoip_provider.yaml

# Huge directories not needed for Go services
rust/
app/